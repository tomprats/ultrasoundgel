- if post.episode
  #{post.episode.author}.
#{post.title}.
<i>Ultrasound G.E.L. Podcast Blog.</i>
- if post.published_at
  Published on #{post.published_at.to_date.to_s(:long)}.
Accessed on #{DateTime.now.to_date.to_s(:long)}.
Available at #{root_url}#{post.episode&.number || Episode.maximum(:number) + 1}.
